{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); echo \"$FILE\" | grep -qE '\\.(ts|tsx)$' && npx tsc --noEmit 2>&1 | head -40 | jq -Rs '{hookSpecificOutput:{hookEventName:\"PostToolUse\",additionalContext:(\"TypeScript check:\\n\" + .)}}' || echo '{}'",
            "statusMessage": "TypeScript check...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
